# Archived — not linked in production builds

This directory preserves the pre-Sapphire-rebuild MelonPrime-only Vulkan bridge.
It is retained for diff reference only and must not be wired into `CMakeLists.txt`.

Production path: `VulkanReference/` + `SapphireGenerated/` + `MELONPRIME_SAPPHIRE_REBUILD`.
