--- a/.claude/rules/plan/metal_compute_shader_phase7_execution.md
+++ b/.claude/rules/plan/metal_compute_shader_phase7_execution.md
@@ -353,3 +353,13 @@
 - summaryのみcommand completion後に共有bufferから読み出し、画像本体のCPU readbackは追加しない。
 - fallback frameでもscreen swap／brightnessをscanline単位で保存。
 - 可視出力はPhase 8HのSoftware 2D ownership pathを維持。
+
+<!-- MELONPRIME_METAL_PHASE8K_DIRECT_SEGMENTED_CUTOVER_V2 -->
+## 2026-07-11 — Phase 8K direct segmented cutover v2
+
+- segmented snapshot bufferを通常renderer stateとして常時有効化。
+- mixed 3D+2D frameをsegmented Metal 2Dへ直接接続。
+- VBlankのfull-frame Metal 2D分岐を削除。
+- Software 2D後のshadow submitを削除。
+- snapshot全行統計、600-frame render統計、experimental visible判定を削除。
+- 診断compare kernelとautomatic fallbackは追加しない。
--- a/src/GPU2D_Metal.mm
+++ b/src/GPU2D_Metal.mm
@@ -3,6 +3,7 @@
 // MELONPRIME_METAL_2D_HUD_PARITY_V1
 // MELONPRIME_METAL_2D_SCANLINE_SNAPSHOT_V1
 // MELONPRIME_METAL_2D_SEGMENTED_SHADOW_RENDER_V1
+// MELONPRIME_METAL_2D_DIRECT_SEGMENTED_CUTOVER_V2
 
 #if defined(MELONPRIME_ENABLE_METAL)
 
@@ -46,20 +47,13 @@
 
 bool MetalSegmented2DVisibleEnabled()
 {
-    static const bool enabled =
-        MetalEnvironmentFlagEnabled(
-            "MELONPRIME_METAL_SEGMENTED_2D_VISIBLE");
-    return enabled;
+    return true;
 }
 
 bool MetalSegmented2DSnapshotEnabled()
 {
-    static const bool enabled =
-        MetalEnvironmentFlagEnabled(
-            "MELONPRIME_METAL_SEGMENTED_2D_CAPTURE") ||
-        MetalSegmented2DShadowEnabled() ||
-        MetalSegmented2DVisibleEnabled();
-    return enabled;
+    // Direct renderer state: no diagnostic environment switch.
+    return true;
 }
 
 constexpr size_t MetalAlignConstantStride(size_t size)
--- a/src/GPU2D_MetalFullGpuMethods.inc
+++ b/src/GPU2D_MetalFullGpuMethods.inc
@@ -4,6 +4,7 @@
 // MELONPRIME_METAL_2D_HUD_PARITY_V1
 // MELONPRIME_METAL_2D_SCANLINE_SNAPSHOT_V1
 // MELONPRIME_METAL_2D_SEGMENTED_SHADOW_RENDER_V1
+// MELONPRIME_METAL_2D_DIRECT_SEGMENTED_CUTOVER_V2
 
 bool MetalRenderer2D::BuildFullGpuPipelines() noexcept
 {
@@ -145,81 +146,6 @@
 
 void MetalRenderer2D::MaybeReportSegmentSnapshotFrame() noexcept
 {
-    if (!State || !State->SnapshotBuffersReady)
-        return;
-
-    for (int line = 0; line < kScreenH; line++)
-    {
-        if (!State->LayerSnapshotValid[line] ||
-            !State->SpriteSnapshotValid[line])
-        {
-            return;
-        }
-    }
-
-    uint32_t layerSegments = 1;
-    uint32_t spriteSegments = 1;
-    uint32_t combinedSegments = 1;
-    uint32_t maxSprites = State->SpriteSnapshotCount[0];
-    uint64_t spriteTotal = State->SpriteSnapshotCount[0];
-
-    auto combinedHash = [&](int line) {
-        uint64_t hash = State->LayerSnapshotHash[line];
-        hash ^= State->SpriteSnapshotHash[line] +
-            0x9E3779B97F4A7C15ull + (hash << 6u) + (hash >> 2u);
-        return hash;
-    };
-
-    uint64_t previousCombined = combinedHash(0);
-    for (int line = 1; line < kScreenH; line++)
-    {
-        if (State->LayerSnapshotHash[line] !=
-            State->LayerSnapshotHash[line - 1])
-        {
-            layerSegments++;
-        }
-        if (State->SpriteSnapshotHash[line] !=
-            State->SpriteSnapshotHash[line - 1])
-        {
-            spriteSegments++;
-        }
-
-        const uint64_t currentCombined = combinedHash(line);
-        if (currentCombined != previousCombined)
-        {
-            combinedSegments++;
-            previousCombined = currentCombined;
-        }
-
-        maxSprites = std::max(
-            maxSprites,
-            State->SpriteSnapshotCount[line]);
-        spriteTotal += State->SpriteSnapshotCount[line];
-    }
-
-    State->SnapshotFrameCount++;
-    if (State->SnapshotFrameCount == 1 ||
-        (State->SnapshotFrameCount % 600u) == 0u)
-    {
-        std::fprintf(
-            stderr,
-            "[MelonPrime] metal segmented 2d snapshot: "
-            "engine=%u frame=%llu lines=%d "
-            "layerSegments=%u spriteSegments=%u combinedSegments=%u "
-            "avgSprites=%.2f maxSprites=%u "
-            "gpuReady=%u visiblePath=ownership-gated-software-2d\\n",
-            GPU2D.Num,
-            static_cast<unsigned long long>(
-                State->SnapshotFrameCount),
-            kScreenH,
-            layerSegments,
-            spriteSegments,
-            combinedSegments,
-            static_cast<double>(spriteTotal) /
-                static_cast<double>(kScreenH),
-            maxSprites,
-            SegmentedSnapshotReady() ? 1u : 0u);
-    }
 }
 
 void MetalRenderer2D::CaptureScanlineState(int line) noexcept
@@ -249,7 +175,6 @@
         }
     }
 
-    MaybeReportSegmentSnapshotFrame();
 }
 
 void MetalRenderer2D::CaptureSpriteScanlineState(int line) noexcept
@@ -306,7 +231,6 @@
         State->SpriteSnapshotLastLine = line;
     }
 
-    MaybeReportSegmentSnapshotFrame();
 }
 
 bool MetalRenderer2D::SegmentedSnapshotReady() const noexcept
@@ -518,16 +442,11 @@
             }
         }
     }
-
-    const bool visibleSegmentedPath =
-        MetalSegmented2DVisibleEnabled();
     id<MTLCommandBuffer> command =
         [State->Queue commandBuffer];
     if (!command)
         return false;
-    command.label = visibleSegmentedPath
-        ? @"MelonPrime Metal Phase 8J Segmented 2D Visible"
-        : @"MelonPrime Metal Phase 8J Segmented 2D Shadow";
+    command.label = @"MelonPrime Metal Phase 8K Segmented 2D";
 
     {
         MTLRenderPassDescriptor* pass =
@@ -586,8 +505,6 @@
 
     const uint32_t scale =
         static_cast<uint32_t>(std::max(1, State->Scale));
-    uint64_t bgDraws = 0;
-    uint64_t spriteDraws = 0;
 
     for (const Metal2DSegmentRange& segment : segments)
     {
@@ -661,7 +578,6 @@
                         vertexStart:0
                         vertexCount:6];
             [encoder endEncoding];
-            bgDraws++;
         }
 
         const NSUInteger scissorY =
@@ -741,7 +657,6 @@
                             vertexCount:6
                           instanceCount:numSprites * 2u];
                 [encoder endEncoding];
-                spriteDraws++;
                 return true;
             };
 
@@ -828,34 +743,6 @@
     }
 
     [command commit];
-
-    State->SegmentedRenderFrames++;
-    State->SegmentedRenderSegments += segments.size();
-    State->SegmentedRenderBgDraws += bgDraws;
-    State->SegmentedRenderSpriteDraws += spriteDraws;
-
-    if (!State->LoggedSegmentedRender ||
-        (State->SegmentedRenderFrames % 600u) == 0u)
-    {
-        State->LoggedSegmentedRender = true;
-        std::fprintf(
-            stderr,
-            "[MelonPrime] metal segmented 2d render: "
-            "engine=%u frame=%llu segments=%zu "
-            "bgDraws=%llu spriteDraws=%llu "
-            "scale=%u visible=%u shadow=%u "
-            "resourceModel=frame-final\n",
-            GPU2D.Num,
-            static_cast<unsigned long long>(
-                State->SegmentedRenderFrames),
-            segments.size(),
-            static_cast<unsigned long long>(bgDraws),
-            static_cast<unsigned long long>(spriteDraws),
-            scale,
-            visibleSegmentedPath ? 1u : 0u,
-            visibleSegmentedPath ? 0u : 1u);
-    }
-
     return true;
 }
 
--- a/src/GPU_Metal.mm
+++ b/src/GPU_Metal.mm
@@ -12,6 +12,7 @@
 // MELONPRIME_METAL_GPU_RESIDENT_2D_V1
 // MELONPRIME_METAL_GPU_DISPLAY_CAPTURE_V1
 // MELONPRIME_METAL_2D_SEGMENTED_SHADOW_RENDER_V1
+// MELONPRIME_METAL_2D_DIRECT_SEGMENTED_CUTOVER_V2
 // MELONPRIME_METAL_2D_SEGMENTED_GPU_DIFF_V1
 // MELONPRIME_METAL_COMPUTE_TEXTURED_RASTER_V1
 
@@ -1274,52 +1275,20 @@
             const bool allowCaptureTextures =
                 !MetalCaptureFrameHadCapture();
 
-            const bool segmentedVisible =
-                MetalSegmented2DVisibleRequested() &&
-                Metal2D_A && Metal2D_B &&
+            rendered = Metal2D_A && Metal2D_B &&
+                capture128 && capture256 &&
                 Metal2D_A->SegmentedFrameComplete() &&
-                Metal2D_B->SegmentedFrameComplete();
-
-            if (segmentedVisible)
-            {
-                rendered = capture128 && capture256 &&
-                    Metal2D_A->RenderSegmentedGpuFrame(
-                        high3D,
-                        capture128,
-                        capture256,
-                        allowCaptureTextures) &&
-                    Metal2D_B->RenderSegmentedGpuFrame(
-                        high3D,
-                        capture128,
-                        capture256,
-                        allowCaptureTextures);
-
-                if (rendered &&
-                    !FullGpuState->LoggedSegmentedVisible)
-                {
-                    FullGpuState->LoggedSegmentedVisible = true;
-                    std::fprintf(
-                        stderr,
-                        "[MelonPrime] metal segmented 2d: "
-                        "experimental visible path active; "
-                        "Software 2D readback disabled\n");
-                }
-            }
-            else
-            {
-                rendered = Metal2D_A && Metal2D_B &&
-                    capture128 && capture256 &&
-                    Metal2D_A->RenderFullGpuFrame(
-                        high3D,
-                        capture128,
-                        capture256,
-                        allowCaptureTextures) &&
-                    Metal2D_B->RenderFullGpuFrame(
-                        high3D,
-                        capture128,
-                        capture256,
-                        allowCaptureTextures);
-            }
+                Metal2D_B->SegmentedFrameComplete() &&
+                Metal2D_A->RenderSegmentedGpuFrame(
+                    high3D,
+                    capture128,
+                    capture256,
+                    allowCaptureTextures) &&
+                Metal2D_B->RenderSegmentedGpuFrame(
+                    high3D,
+                    capture128,
+                    capture256,
+                    allowCaptureTextures);
 
             if (rendered)
             {
--- a/src/GPU_MetalFullGpuMethods.inc
+++ b/src/GPU_MetalFullGpuMethods.inc
@@ -3,6 +3,7 @@
 // MELONPRIME_METAL_MIXED_3D_2D_OPENGL_PARITY_GUARD_V1
 // MELONPRIME_METAL_GPU_DISPLAY_CAPTURE_V1
 // MELONPRIME_METAL_2D_SEGMENTED_SHADOW_RENDER_V1
+// MELONPRIME_METAL_2D_DIRECT_SEGMENTED_CUTOVER_V2
 // MELONPRIME_METAL_2D_SEGMENTED_GPU_DIFF_V1
 
 namespace
@@ -50,25 +51,12 @@
 
 bool MetalSegmented2DVisibleRequested()
 {
-    static const bool enabled = []() {
-        const char* value =
-            std::getenv(
-                "MELONPRIME_METAL_SEGMENTED_2D_VISIBLE");
-        return value && value[0] == '1';
-    }();
-    return enabled;
+    return true;
 }
 
 bool MetalAllowExperimentalMixed3D2DGpuPath()
 {
-    static const bool legacyEnabled = []() {
-        const char* value =
-            std::getenv(
-                "MELONPRIME_METAL_EXPERIMENTAL_MIXED_3D_2D");
-        return value && value[0] == '1';
-    }();
-    return legacyEnabled ||
-        MetalSegmented2DVisibleRequested();
+    return true;
 }
 
 bool MetalEngineAUsesMixed3DAnd2DOverlay(const GPU& gpu)
@@ -291,39 +279,6 @@
     if (!GPU.CaptureEnable)
         FullGpuState->BlockedByCaptureFeedback = false;
 
-    const bool mixed3D2D =
-        MetalEngineAUsesMixed3DAnd2DOverlay(GPU);
-    const bool mixedOverride =
-        MetalAllowExperimentalMixed3D2DGpuPath();
-
-    if (mixed3D2D && !mixedOverride &&
-        !FullGpuState->LoggedMixed3D2DFallback)
-    {
-        FullGpuState->LoggedMixed3D2DFallback = true;
-        std::fprintf(
-            stderr,
-            "[MelonPrime] metal OpenGL parity guard: "
-            "mixed 3D+2D overlay detected; using Software 2D compositor "
-            "while keeping the selected Metal 3D renderer "
-            "(layerEnable=0x%02x dispCnt=0x%08x). "
-            "Set MELONPRIME_METAL_EXPERIMENTAL_MIXED_3D_2D=1 "
-            "only for diagnostics.\n",
-            static_cast<unsigned>(GPU.GPU2D_A.LayerEnable),
-            static_cast<unsigned>(GPU.GPU2D_A.DispCnt));
-    }
-    else if (mixed3D2D && mixedOverride &&
-             !FullGpuState->LoggedMixed3D2DOverride)
-    {
-        FullGpuState->LoggedMixed3D2DOverride = true;
-        std::fprintf(
-            stderr,
-            "[MelonPrime] metal OpenGL parity guard: "
-            "experimental mixed 3D+2D GPU compositor forced "
-            "(layerEnable=0x%02x dispCnt=0x%08x).\n",
-            static_cast<unsigned>(GPU.GPU2D_A.LayerEnable),
-            static_cast<unsigned>(GPU.GPU2D_A.DispCnt));
-    }
-
     FullGpuState->FrameActive =
         !FullGpuState->BlockedByCaptureFeedback &&
         MetalCaptureResourcesCoherent() &&
