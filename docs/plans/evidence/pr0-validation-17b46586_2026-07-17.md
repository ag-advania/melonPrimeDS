# PR-0 validation evidence — HEAD `17b46586`

**Date:** 2026-07-17  
**Host:** macOS 15.7.7 (Build 24G720), Intel Core i5-8259U @ 2.30GHz (x86_64)  
**Instruction:** `docs/plans/melonPrimeDS_develop_metal_完全Metal化_実行指示書_17b46586_2026-07-17.md` §6

## 6.3 Static check

Command:

```zsh
python3 tools/ci/audits/audit-metal-output-state-publication.py
```

Result: **PASS**

Verified:

- `LoadOutputState` / `StoreOutputState` / `ExchangeOutputState` present
- `std::atomic_load/store/exchange_explicit` used for publication
- no live `std::exchange(OutputState, …)`
- no live `GpuCaptureInFlight` bool field
- `GpuWritesInFlight` / `LatestGpuSubmittedToken` / `NextGpuWriteToken` present
- `GetOutput()` falls back to `SoftRenderer::GetOutput()` (no raw `MetalTexture` return)

## 6.4 Build matrix (this host)

| Config | Tree | Result |
|---|---|---|
| Metal ON Release | `build-mac-metal` (`MELONPRIME_ENABLE_METAL=ON`) | PASS (`ninja: no work to do` / prior full link OK) |
| Metal OFF Release | `build-mac` (`MELONPRIME_ENABLE_METAL=OFF`) | PASS (full rebuild + link) |
| FORCE_DISABLE | `build-mac-metal-force-disabled` (`ENABLE_METAL=ON` + `FORCE_DISABLE_METAL=ON`) | PASS |
| macOS ARM64 | — | **N/A on this Intel host** |
| Windows | — | **not run** |
| Linux | `build-linux` | **blocked** (CMakeCache path `/mnt/mp/build-linux` from another machine) |

Binary sizes observed:

- `build-mac-metal/.../melonPrimeDS` ≈ 12.0 MiB
- `build-mac/.../melonPrimeDS` ≈ 11.6 MiB
- `build-mac-metal-force-disabled/.../melonPrimeDS` ≈ 11.4 MiB

## 6.5 / 6.6 Unresolved

- Thread Sanitizer tree + 1000-cycle scale stress: not configured in this session
- Capture multi-command callback ordering harness: not added yet (covered conceptually by writer token code; needs automated test)
- Real ROM play / Metal Validation / GPU Frame Capture: not run

## Gate decision

PR-0 **partially accepted** for continuing to PR-1 on this Intel macOS host with Metal ON/OFF/FORCE_DISABLE compile evidence and static contract audit.

Remaining PR-0 items (TSan, Windows/Linux, callback harness, ROM stress) stay open and are tracked in the progress doc; they must not be silently marked complete.
